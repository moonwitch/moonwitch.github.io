# Never commit state or secrets
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
*.tfvars
!*.tfvars.example
crash.log

# Rendered at runtime from backend.tf.example (keep the .example committed)
backend.tf
